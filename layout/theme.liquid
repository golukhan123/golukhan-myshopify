<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }}</title>
  <meta name="description" content="Welcome to {{ shop.name }} - Your go-to store for high-quality products.">

  <!-- Include any additional Shopify header content -->
  {{ content_for_header }}

  <!-- Stylesheets -->
  <link rel="stylesheet" href="{{ 'tailwind.css' | asset_url }}">
  <link rel="stylesheet" href="{{ 'styles.css' | asset_url }}">
</head>

<body>
  <!-- Header -->
  {% render 'header' %}

  <!-- Main Content -->
  <main>{{ content_for_layout }}</main>

  <!-- Footer -->
  {% render 'footer' %}
</body>
</html>
