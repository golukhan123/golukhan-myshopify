<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="">
    <link rel="canonical" href="{{ canonical_url }}">

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'meta-tags' %}

    <meta name="description" content="Welcome to {{ shop.name }} - Your go-to store for high-quality products.">
  
    {{ content_for_header }}
  
    <link rel="stylesheet" href="{{ 'css/tailwind.css' | asset_url }}">
    <link rel="stylesheet" href="{{ 'css/styles.css' | asset_url }}">
  
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.css">
    
    <script src="https://code.jquery.com/jquery.min.js"></script>
    <script src="{{ 'js/theme.js' | asset_url }}"></script>
  
    <script src="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-element-bundle.min.js"></script>

  </head>
  
  <body>
    {% render 'header' %}
  
    <main>{{ content_for_layout }}</main>
    
    {% render 'footer' %}
  
  </body>
  </html>

  {% comment %} <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>{{ shop.name }}</title>
      <meta name="description" content="Welcome to {{ shop.name }} - Your go-to store for high-quality products.">
    
      <!-- Shopify Header Content -->
      {{ content_for_header }}
    
      <!-- Stylesheets -->
      <link rel="stylesheet" href="{{ 'css/tailwind.css' | asset_url }}">
      <link rel="stylesheet" href="{{ 'css/styles.css' | asset_url }}">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.css">
    
      <!-- Scripts -->
      <!-- Load jQuery -->
      <script src="https://code.jquery.com/jquery-3.6.4.min.js" integrity="sha384-oUmVI5RtobzM84exuZlfyRDtt4G7z9zDXxgxlBa2ajTwAi/1yZQXb3G2LGKvKeSu" crossorigin="anonymous" defer></script>
    
      <!-- Theme Scripts -->
      <script src="{{ 'js/theme.js' | asset_url }}" defer></script>
    
      <!-- Swiper Scripts -->
      <script src="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.js" defer></script>
    </head>
    
    <body>
      {% render 'header' %}
    
      <main>{{ content_for_layout }}</main>
    
      {% render 'footer' %}
    </body>
    </html> {% endcomment %}


  
