<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }}</title>
  <meta name="description" content="Welcome to {{ shop.name }} - Your go-to store for high-quality products.">

  {{ content_for_header }}

  <link rel="stylesheet" href="{{ 'css/tailwind.css' | asset_url }}">
  <link rel="stylesheet" href="{{ 'css/styles.css' | asset_url }}">

  <script src="https://code.jquery.com/jquery.min.js"></script>
  <script src="{{ 'js/theme.js' | asset_url }}"></script>
</head>

<body>
  {% render 'header' %}

  <main>{{ content_for_layout }}</main>
  
  {% render 'footer' %}
</body>
</html>
